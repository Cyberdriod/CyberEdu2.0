package cyberdroid.jobportal.dao;

import java.util.List;

import org.springframework.stereotype.Service;

import cyberdroid.jobportal.beans.Profile;
import cyberdroid.jobportal.beans.Studexperience;

@Service
public interface SignUpDAO {

	void save(Profile profile);
	void edudelete(int eduid);
	void saveexperience(Studexperience exp);
	/*
	 * List<Profile> getProfileById(Integer userId); void
	 * updatePersonalDetailsById(Profile profile, Integer userId); void
	 * updateOfficeDetailsById(Profile profile, Integer userId); List<Profile>
	 * existUser(String userEmail, String mobile);
	 */
}
